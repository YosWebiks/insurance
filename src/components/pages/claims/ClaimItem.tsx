import React from 'react'
import { Claim } from '../../../types/redux'

interface Props {
  claim:Claim
}

export default function ClaimItem({claim}:Props) {
  return (
    <div>ClaimItem</div>
  )
}
